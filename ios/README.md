# CanvasFlow iOS

Native SwiftUI shell for CanvasFlow, loading the production web app at https://canvasios.vercel.app.

Bundle identifier: `com.canvasflow.app`

This folder is prepared for a CodeMagic/Xcode iOS build. Configure the iOS workflow with an Xcode project/workspace and Apple signing credentials before producing an IPA.
