{
  "name": "canvasflow-capacitor",
  "version": "1.0.0",
  "private": true,
  "description": "CanvasFlow whiteboard wrapped with Capacitor for iOS cloud builds",
  "scripts": {
    "cap:add:ios": "npx cap add ios",
    "cap:sync": "npx cap sync ios",
    "cap:doctor": "npx cap doctor"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  }
}
